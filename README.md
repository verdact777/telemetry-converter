# Telemetry Converter

A Streamlit-based device telemetry data converter.

Converts between two raw JSON formats into a unified structure.

## Run

```bash
pip install -r requirements.txt
streamlit run python-app/app.py --server.port 5000
```
